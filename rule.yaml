apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: e128b620-89ab-41cf-9abd-0266cabca4a5
  namespace: default
spec:
  selector:
    matchLabels:
      app: nginx
  action: ALLOW
  rules:
  - from:
    - source:
        ipBlocks: ["0.0.0.0/0"]
